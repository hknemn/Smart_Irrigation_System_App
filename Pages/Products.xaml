<Page x:Class="Smart_Irrigation_System.Pages.Products"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Products">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" Background="LightGray">
            <Label Content="Aranacak ürünü giriniz:" VerticalContentAlignment="Center" FontSize="14"/>
            <TextBox x:Name="searchTextBox" Width="200" VerticalContentAlignment="Center" FontSize="14" Margin="0,10,0,10" Height="30"/>
            <Button x:Name="createProductButton" Content="Ürün Ekle" Width="100" FontSize="14" Margin="0,10" Height="40" Click="createProductButton_Click"/>
            <Button x:Name="deleteProductButton" Content="Ürün Sil" Width="100" FontSize="14" Margin="0,10" Height="40"/>
            <Button x:Name="updateProductButton" Content="Ürün Güncelle" Width="100" FontSize="14" Margin="0,10" Height="40"/>
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Vertical">
            <ListView x:Name="productsListView">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                        <GridViewColumn Header="Minimum Temperature" DisplayMemberBinding="{Binding Min_Temperature}" Width="130"/>
                        <GridViewColumn Header="Maximum Temperature" DisplayMemberBinding="{Binding Max_Temperature}" Width="130"/>
                        <GridViewColumn Header="Minimum Humidity" DisplayMemberBinding="{Binding Min_Humidity}" Width="120"/>
                        <GridViewColumn Header="Maximum Humidity" DisplayMemberBinding="{Binding Max_Humidity}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
